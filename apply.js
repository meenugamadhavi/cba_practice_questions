// 1. Find the maximum number in an array using apply
// Parameters: numbers (array)
// Test cases: maxNumber([3, 7, 1, 9]) => 9, maxNumber([-5, -1, -3]) => -1
const maxNumber = function (numbers) {};

// 2. Concatenate two arrays
// Parameters: array1, array2
// Test cases: concatArrays([1, 2], [3, 4]) => [1, 2, 3, 4]
const concatArrays = function (array1, array2) {};

// 3. Calculate the sum of numbers in an array
// Parameters: numbers (array)
// Test cases: sumArray([2, 4, 6]) => 12, sumArray([10, 20, 30]) => 60
const sumArray = function (numbers) {};

// 4. Use apply to determine the minimum number in an array
// Parameters: numbers (array)
// Test cases: minNumber([3, 7, 1, 9]) => 1, minNumber([-5, -1, -3]) => -5
const minNumber = function (numbers) {};

// 5. Calculate the average of numbers in an array
// Parameters: numbers (array)
// Test cases: averageOfArray([2, 4, 6]) => 4, averageOfArray([10, 20, 30]) => 20
const averageOfArray = function (numbers) {};

// 6. Borrow a method to reverse an array
// Parameters: numbers (array)
// Test cases: reverseArray([1, 2, 3]) => [3, 2, 1]
const reverseArray = function (numbers) {};

// 7. Use apply to flatten a nested array
// Parameters: nestedArray (array)
// Test cases: flattenArray([[1, 2], [3, 4]]) => [1, 2, 3, 4]
const flattenArray = function (nestedArray) {};

// 8. Filter out even numbers from an array
// Parameters: numbers (array)
// Test cases: filterEvens([1, 2, 3, 4]) => [2, 4]
const filterEvens = function (numbers) {};

// 9. Find the longest word in an array
// Parameters: words (array)
// Test cases: findLongestWord(["hello", "world", "javascript"]) => "javascript"
const findLongestWord = function (words) {};

// 10. Calculate the product of all numbers except one in an array
// Parameters: numbers (array)
// Test cases: productExceptSelf([1, 2, 3]) => [6, 3, 2]
const productExceptSelf = function (numbers) {};
